Git init (to initualize git depository in the root folder)
Git status  (to check status of the file)
git commit (For adding a message/comment on a file that you have pushed on the repository)
Git add (for adding files to a repo)
git push (for pushing files to repository)
Git merge (to merge branches together)
git clone (for copying a remote repository  on your machine)
Git pull (for synchronising the remote repository files with your files on localhos)
git checkout (For switching branch)
Git log (For viewing log files on a repository)
git remote -v (For checking the remote url of a repository)
Git branch (For checking current branch on the repo)
git remote set-url (For setting/changing  a remote url to a repository)
